import { green, grey, red, yellow } from '@mui/material/colors'

export const ACTION_STATE_COLORS = {
  created: grey[500],
  running: yellow[700],
  finished: green[500],
  error: red[500],
  canceled: grey[500],
}
